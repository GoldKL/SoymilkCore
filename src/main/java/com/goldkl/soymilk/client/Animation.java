package com.goldkl.soymilk.client;

import com.goldkl.soymilk.SoymilkCore;
/*
import dev.kosmx.playerAnim.api.layered.IAnimation;
import dev.kosmx.playerAnim.api.layered.KeyframeAnimationPlayer;
import dev.kosmx.playerAnim.api.layered.ModifierLayer;
import dev.kosmx.playerAnim.minecraftApi.PlayerAnimationAccess;
import dev.kosmx.playerAnim.minecraftApi.PlayerAnimationRegistry;*/
import net.minecraft.client.player.AbstractClientPlayer;
import net.minecraft.resources.ResourceLocation;

public class Animation {
    /*
    static boolean doAnimation(AbstractClientPlayer player, ResourceLocation animation) {
        if (player == null) return false;
        ModifierLayer<IAnimation> ianimation = (ModifierLayer<IAnimation>) PlayerAnimationAccess.getPlayerAssociatedData( player).get(new ResourceLocation(SoymilkCore.MODID, "animation"));
        if (animation != null) {
            ianimation.setAnimation(new KeyframeAnimationPlayer(PlayerAnimationRegistry.getAnimation(animation)));
        }
        return false;
    }*/
}
